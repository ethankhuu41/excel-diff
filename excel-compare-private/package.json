{
  "name": "excel-compare-private",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --minify --sourcemap --format=iife --outfile=public/app.js",
    "serve": "npx http-server public -p 8080 --no-dotfiles --cache -1"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "xlsx": "^0.18.5"
  }
}
